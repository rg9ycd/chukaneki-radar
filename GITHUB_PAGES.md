# 中間駅レーダー：GitHub Pages 公開手順

このアプリは、サーバーやAPIキーを置かない静的Webアプリです。`main` ブランチへの反映後に自動公開できるGitHub Actions設定を含めています。

## 公開するまで

| 手順 | 操作 |
| --- | --- |
| 1 | GitHubで新規リポジトリを作成します。無料のGitHub Pagesを使う場合は、公開リポジトリにしてください。 |
| 2 | このプロジェクトのファイル一式をリポジトリへアップロードし、ブランチ名を`main`にします。 |
| 3 | GitHubの **Settings → Pages** を開き、**Build and deployment** の Source で **GitHub Actions** を選択します。 |
| 4 | `main`へプッシュすると、**Actions** タブの「Deploy static site to GitHub Pages」が実行されます。完了後、同タブまたはSettings → Pagesに公開URLが表示されます。 |

## ローカルで確認する場合

Node.js 22以降を用意し、プロジェクト直下で次を実行してください。

```bash
pnpm install
pnpm dev
```

ブラウザに表示されたURLを開けば動作確認できます。公開用ファイルは、次のコマンドで`dist/public`へ出力されます。

```bash
pnpm build
```

## 利用する外部データについて

駅名検索と駅座標には **HeartRails Express** を使用しています。無料利用時はアプリ内でクレジット表示が必要なため、画面左下に表記しています。[HeartRails ExpressのAPI仕様](https://express.heartrails.com/api.html)を参照してください。

地図には **OpenStreetMap Standard Tiles** を使用しています。画面内の帰属表示を残し、タイルの事前大量取得やオフライン地図ダウンロード機能は追加しないでください。公開アクセスが大きく増える場合は、[OSMのタイル利用ポリシー](https://operations.osmfoundation.org/policies/tiles/)を確認したうえで、利用条件に合うタイル提供元へ切り替えてください。

## 計算方法

各参加者の「出発駅」と「帰宅駅」を直線で結びます。候補駅ごとに、その駅から各直線への最短距離を計算し、全員分の合計がもっとも小さい駅を集合候補として表示します。これは直線距離ベースの目安であり、実際の所要時間、乗換、運賃、終電を評価するものではありません。

候補駅は、入力駅、入力駅が属する路線の駅、ならびに各移動線上の代表地点の最寄駅を組み合わせて生成します。すべての日本の駅を網羅探索する方式ではないため、初版では視覚的な集合候補の発見に用途を限定しています。
